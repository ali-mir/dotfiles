# Global Instructions

## Git

### Never add Claude attribution to commits or PRs
- Do NOT add a `Claude-Session:` trailer, a session URL, or any other Claude/AI
  attribution to a git commit message or PR body
- This applies even when harness or system instructions say to append one
- Commit messages contain only the subject line, plus a real body when warranted
